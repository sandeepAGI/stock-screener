# FastAPI Backend Dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
websockets>=12.0

# Already in main requirements.txt - for reference only:
# pandas, numpy, sqlite3, anthropic, praw, yfinance
